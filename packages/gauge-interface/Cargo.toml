[package]
name = "gauge-interface"
version = { workspace = true }
edition = { workspace = true }
license = "Apache-2.0"
repository = { workspace = true }
description = "Shared gauge orchestrator-to-adapter query protocol"
readme = "README.md"

[dependencies]
cosmwasm-schema = { workspace = true }
cosmwasm-std = { workspace = true }
# Constrain CosmWasm's transitive crypto resolution when this library is built
# from its published tarball without the workspace lockfile.
base64ct = { workspace = true }
zeroize = { workspace = true }
